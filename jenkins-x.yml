buildPack: maven
pipelineConfig:
  pipelines:
    overrides:
      - pipeline: release
        stage: build
        name: mvn-deploy
        steps:
          - command: mvn -Pprod clean deploy
            dir: /workspace/source
            image: maven
            name: build-mvn-deploy
        containerOptions:
          resources:
            limits:
              cpu: '1'
              memory: 2Gi
