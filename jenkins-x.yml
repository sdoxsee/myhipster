buildPack: maven
pipelineConfig:
  pipelines:
    overrides:
      - pipeline: release
        stage: build
        containerOptions:
          resources:
            limits:
              cpu: 1
              memory: 2Gi
