buildPack: maven
pipelineConfig:
  pipelines:
    overrides:
      - pipeline: release
        stage: build
        name: build-mvn-deploy
        containerOptions:
          resources:
            limits:
              cpu: 1
              memory: 2Gi
